import Vue from 'vue'
import VueRouter from 'vue-router'

// 配置路由
Vue.use(VueRouter)

// 配置路由表
const routes = [

]

// 创建router实例
const router = new VueRouter({
  routes
})

// 导出router实例
export default router
